# Audit Reports

Durable audit summaries live here. Reports should describe the tested runtime,
the commands that passed or failed, residual risk, and follow-up work.

Disposable screenshots, Playwright traces, logs, and run output belong in the
ignored `artifacts/` directory instead.
